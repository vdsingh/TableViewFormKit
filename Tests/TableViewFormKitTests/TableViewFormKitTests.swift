import XCTest
@testable import TableViewFormKit

final class TableViewFormKitTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(TableViewFormKit().text, "Hello, World!")
    }
}
